<div class="fieldset rewards_referral_information">
    <h2 class="legend"><?php echo $this->__('Referral Information') ?></h2>
    <p>Optional – If a friend referred you to our website, input their email address or referral code here. For referring you to our website, they will earn rewards points when you mention them here.</p>
    <ul class="form-list">
        <li class="fields">
            <div class="field">
                <label for="rewards_referral">
                    <?php
                    if($this->showReferralEmail() && ! $this->showReferralCode() ) {
                        echo $this->__('Referral E-mail');
                    }elseif(!$this->showReferralEmail() && $this->showReferralCode()  ) {
                        echo $this->__('Referral Code');
                    } else {
                        echo $this->__('Referral E-mail or Code');
                    }
                    ?>
                </label>
                <div class="input-box">
                    <input type="text" name="rewards_referral" id="rewards_referral" class="input-text" value="<?php echo $this->getCurrentAffiliate(); ?>" />
                </div>
            </div>
        </li>
    </ul>
</div>
